using System.Collections.Generic;

namespace LearnAlgorithm;

public class LearnQuery
{
    private int[] datas = new[] { 1, 2, 3, 4 };

    public LearnQuery()
    {
        LinkedList<int> deque = new LinkedList<int>();
        // 1 输入受限双端队列
        // 一端只能删除（队头），一端可以进行插入删除（此端为队尾）
        // 一个元素可以进行插入操作，此时也可以进行删除操作，删除操作可以在两边分别进行

    }

    private void InitData()
    {

    }
}